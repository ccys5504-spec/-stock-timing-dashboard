# 국내 주식 매수/매도 타이밍 분석 프로그램 — 개발 프롬프트

아래 내용을 그대로 Claude Code(또는 다른 코딩 AI)에게 전달하면 프로그램을 만들 수 있습니다.
필요하면 세부 항목(종목, 지표, 기간 등)만 수정해서 사용하세요.

---

## 프롬프트 시작

국내 주식(코스피/코스닥) 종목의 매수/매도 타이밍을 분석하고 백테스트해주는
**웹 대시보드**를 만들어줘. 아래 사양대로 구현해줘.

### 1. 전체 목표
- 과거 주가 데이터를 기반으로 매수/매도 신호를 계산하고, 그 신호로 실제 매매했다면
  수익률이 어땠을지 **백테스트**로 검증한다.
- 최신 데이터 기준 "현재 시점 신호"(매수/매도/보유)도 함께 보여준다.
- **실제 주문은 실행하지 않는다.** 어디까지나 분석/의사결정 보조 도구다.

### 2. 기술 스택
- 언어: Python 3.11+
- 데이터: `pykrx` 또는 `FinanceDataReader` (국내 상장 종목의 일봉 시세 무료 제공)
- 지표 계산: `pandas`, `numpy`, `ta`(technical analysis 라이브러리)
- 대시보드: `Streamlit`
- 차트: `plotly` (캔들차트 + 지표 오버레이)

### 3. 매수/매도 신호 로직 (추천 조합)
아래 지표들을 계산해서 **점수 합산 방식**으로 신호를 만들어줘 (지표 하나만 보고
판단하면 오신호가 많기 때문):

1. **이동평균선 골든/데드크로스**: 20일선이 60일선을 상향 돌파 = 매수(+1),
   하향 돌파 = 매도(-1)
2. **RSI(14)**: 30 이하 = 과매도(매수 +1), 70 이상 = 과매수(매도 -1)
3. **MACD**: MACD선이 시그널선을 상향 돌파 = 매수(+1), 하향 돌파 = 매도(-1)
4. **볼린저밴드(20, 2σ)**: 하단 밴드 터치 후 반등 = 매수(+1), 상단 밴드 터치 후
   하락 = 매도(-1)

점수 합계가 **+2 이상 → "매수" 신호**, **-2 이하 → "매도" 신호**, 그 사이는
"관망"으로 표시. (임계값은 나중에 조절 가능하게 상수로 분리해줘)

### 4. 백테스트 기능
- 신호대로 매매했을 때의:
  - 누적 수익률
  - 최대 낙폭(MDD)
  - 승률(수익 거래 비율)
  - 총 매매 횟수
  - **단순 보유(Buy & Hold)와 비교 수익률** (반드시 비교 기준으로 제시)
- 기간은 사용자가 대시보드에서 선택 가능하게 (예: 최근 1년/3년/5년)

### 5. 대시보드 UI (Streamlit)
- 종목 검색/선택 (종목명 또는 종목코드 입력)
- 캔들차트 + 이동평균선/볼린저밴드 오버레이
- RSI, MACD 서브 차트
- 매수/매도 신호 지점을 차트에 화살표/마커로 표시
- 백테스트 결과 요약 카드 (수익률, MDD, 승률 등)
- 현재 시점의 신호 상태를 눈에 띄게 표시 (예: 큰 배지로 "매수/매도/관망")

### 6. 프로젝트 구조 (제안)
```
주식매매 타이밍/
├── app.py               # Streamlit 진입점
├── data/
│   └── fetch.py          # 시세 데이터 수집
├── signals/
│   └── indicators.py     # 지표 계산 및 신호 로직
├── backtest/
│   └── engine.py          # 백테스트 엔진
├── requirements.txt
└── README.md
```

### 7. 필수 주의사항 (반드시 코드/UI에 반영)
- 대시보드 화면에 **"투자 참고용이며, 투자 자문이 아닙니다. 과거 성과가 미래
  수익을 보장하지 않습니다"** 문구를 눈에 띄게 표시할 것
- 실제 매수/매도 주문, 증권사 API 연동, 자동매매 기능은 포함하지 않는다
- 데이터 수집 실패(종목코드 오류, 네트워크 오류 등)에 대한 예외 처리 포함

### 8. 실행 방법 안내
`requirements.txt`와 함께, 아래처럼 실행할 수 있도록 README에 설명을 넣어줘:
```bash
pip install -r requirements.txt
streamlit run app.py
```

## 프롬프트 끝

---

### 참고: 이후 확장하고 싶다면
- 특정 종목 관심목록에 대해 매일 신호를 계산해 텔레그램/이메일로 알림 보내기
- 지표 조합을 여러 개 만들어 백테스트로 성과 비교(파라미터 최적화)
- 증권사 API(키움 Open API+, 한국투자증권 API 등) 연동한 자동매매는 별도 단계로,
  실제 주문 실행 전 반드시 모의투자로 충분히 검증할 것
