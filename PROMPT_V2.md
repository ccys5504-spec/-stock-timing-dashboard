# 국내 주식 대시보드 v2 — 전략 구조 개편 프롬프트 (2026-09-15)

이 문서는 사용자가 제시한 개선안(외부 제안 문서)을 검토하고, **어디까지 채택할지
판단한 결과**를 실행 가능한 스펙으로 정리한 것입니다. `PROMPT.md`가 v1(현재
지표 합산 타이밍 전략)을 만든 프롬프트였다면, 이 문서는 v2(포트폴리오형 상대강도
전략)를 만들기 위한 프롬프트입니다. 아직 구현 전 — 이 문서에 대한 승인 후
실제 코드 작업을 시작합니다.

---

## 왜 구조를 바꾸는가

2026-09-15 2단계 워크포워드 검증 결과, 지금의 "지표 4개 합산 + 파라미터
최적화" 방식은 실전 기대수익이 매우 낮습니다 — 훈련 구간만 보고 고른 파라미터를
진짜 미래(검증 구간)에 적용하면 평균 +7.0%로, 같은 기간 단순보유 +60.1%에 크게
못 미쳤습니다(`README.md` "워크포워드 검증" 절 참고). **파라미터를 더 정교하게
튜닝하는 방향은 한계에 도달했다고 판단합니다.**

사용자가 제시한 제안 문서의 핵심 주장은 "언제 살까"보다 "무엇을 살까"가 더
중요하다는 것이고, 방향 자체는 타당합니다. 다만 제안 문서도 스스로 인정하듯
국내시장 모멘텀 효과는 학술적으로 엇갈리는 결과가 있어 **"검증된 사실"이 아니라
"최우선으로 검증할 가설"**로 다룹니다.

## 채택 판단

제안 문서의 7개 아이디어를 우리가 가진 데이터(FinanceDataReader의 OHLCV)만으로
지금 바로 구현·검증할 수 있는지 기준으로 나눴습니다.

| # | 아이디어 | 판단 | 이유 |
|---|---|---|---|
| 1 | 상대강도 포트폴리오 | **Phase A 채택** | OHLCV만으로 계산 가능 |
| 2 | 추세/역추세 신호 분리 | **Phase A 채택** | 기존 지표 재활용, 로직만 재설계 |
| 3 | 복수 시간대 추세 | **Phase A 채택** | OHLCV만으로 계산 가능 |
| 4 | ATR 기반 동적 청산 | **Phase A 채택** | OHLCV(고가/저가)로 계산 가능 |
| 5 | 변동성 기반 비중 조절 | **Phase A 채택** | OHLCV로 계산 가능 |
| 6 | 시장 지수 기반 장세 필터 | **Phase A 채택** | KOSPI/KOSDAQ 지수도 FDR로 조회 가능 |
| 7 | 재무 품질 필터 | **Phase C 보류** | FDR엔 재무데이터 없음 — DART 공시 API 등 새 데이터원 필요(아래 참고) |
| — | 생존편향 완전 제거 | **보류, 한계로 계속 명시** | 과거 시점별 상장폐지 종목 목록을 무료로 구하기 어려움 |

**PBO/Deflated Sharpe 같은 통계적 엄밀성 도구는 Phase B로 미룹니다** — Phase A
설계 자체가 (아래 서술하듯) 룩백 기간 등을 문헌 표준값으로 고정하고 그리드
탐색을 하지 않는 방식이라, `optimize.py`가 빠졌던 "조합을 늘릴수록 과최적화
위험이 커지는" 함정을 애초에 피하도록 설계했습니다. 그래도 최종 채택 전에는
비용 스트레스 테스트까지는 Phase A에 포함합니다.

---

## Phase A — 지금 구현할 것

### A1. 새 백테스트 엔진: 포트폴리오 리밸런싱 방식

기존 `backtest/engine.py`는 종목 1개, 신호 1개짜리 진입/청산 엔진입니다. 이건
그대로 두고(기존 대시보드가 계속 쓰므로 건드리지 않음), **새 모듈**
`backtest/portfolio_engine.py`를 만듭니다.

- 리밸런싱 주기: **월 1회** (말일 기준)
- 각 리밸런싱 시점에 그 시점까지의 데이터만으로(미래 데이터 사용 금지):
  1. 유니버스 산정 (A2)
  2. 상대강도 순위 계산 (A2)
  3. 상위 K개 종목 선정 (기본 K=15)
  4. 시장 장세 필터로 총 노출 비중 결정 (A5)
  5. 종목별 비중은 위험균등(inverse-volatility) 방식으로 산정 (A4), 종목당
     상한 12%
- 리밸런싱 사이 기간에는 매일:
  - 각 보유 종목에 ATR 트레일링 스탑 체크 (A3) — 걸리면 즉시 청산, 현금 보유
    (다음 리밸런싱까지 재진입하지 않음 — 잦은 재진입으로 회전율이 튀는 것
    방지)
  - 상대강도 순위가 미리 정한 컷오프(예: 상위 40위) 밖으로 완전히 밀려나면
    다음 리밸런싱에서 자동 교체 (매 리밸런싱마다 순위 재계산이므로 별도 로직
    불필요)
- 체결가는 기존 엔진과 동일한 원칙 유지: **신호/판단에 쓴 날의 다음 거래일
  시가**로 체결 (룩어헤드 금지 원칙 재사용)
- 거래비용은 기존과 동일 `ROUND_TRIP_COST`를 재사용하되, 매도뿐 아니라 매수
  시점에도 명시적으로 반영(포트폴리오 엔진은 리밸런싱마다 다수 종목을 동시에
  사고팔므로 회전율이 눈에 보이게 커짐 — 비용 영향이 기존 엔진보다 훨씬 큼)

### A2. 유니버스 & 상대강도 순위

새 모듈 `strategy/momentum.py`:

- 유니버스: `data.fetch.get_universe()` 재사용, 시가총액 상위 200~300개
  (KOSPI+KOSDAQ), 우선주/관리종목/스팩은 기존 로직으로 이미 제외됨. 여기에
  추가로 **최소 거래대금 하한**(예: 최근 20일 평균 거래대금 5억원 이상)을
  걸러서 저유동성 종목 제외.
- 상대강도 점수: 최근 3개월·6개월·12개월 수익률을 각각 계산해 단순평균(또는
  가중평균 — 구현 시 비교). **최근 1개월은 계산에서 제외**(skip-1-month —
  단기 반전 효과를 줄이기 위한 문헌 표준 관행).
- 각 리밸런싱 시점에 상장 후 최소 1년(12개월+skip 계산에 필요한 만큼) 이상
  데이터가 있는 종목만 순위 계산 대상에 포함.
- ⚠️ **생존편향 한계 재확인**: `get_universe()`는 오늘 시점 시가총액 목록만
  준다. 과거 리밸런싱 시점에 실제로 상위 200~300위 안에 있었지만 지금은
  상장폐지·순위 이탈된 종목은 이 백테스트에 반영되지 않는다 — 결과가 실제보다
  낙관적일 수 있다는 점을 README/앱에 지금의 3종목 백테스트와 마찬가지로
  명시한다.

### A3. ATR 기반 청산

`signals/indicators.py`에 ATR(Average True Range, 기본 14일) 계산 함수 추가.

- 초기 손절: 진입가 − 2.5×ATR(진입일 기준)
- 트레일링: 보유 중 **전일 종가까지의 최고가** − 3.5×ATR(그 시점 ATR) — 기존
  `backtest/engine.py`의 "전일 확정 기준선 + 당일 저가로 판정" 원칙을 그대로
  계승
- ATR 배수(2.5, 3.5)는 종목별로 따로 맞추지 않고 전체 유니버스에 고정 적용
  (파라미터 각개 최적화로 인한 과최적화 방지 — 이 프로젝트가 이미 겪은 실수를
  반복하지 않기 위함)

### A4. 위험균등(변동성 역가중) 비중

- 각 종목의 최근 60일 일간수익률 표준편차(σ)를 구해 `weight ∝ 1/σ`로 1차
  배분 후 정규화
- 종목당 비중 상한 12%, 이를 넘으면 초과분을 나머지 종목에 비례 재분배
- 업종(시장 구분: KOSPI/KOSDAQ 정도의 단순 구분부터 시작 — 업종 세분류는
  FDR로 바로 얻기 어려우므로 Phase A에서는 생략, 필요성이 확인되면 Phase B로)

### A5. 시장 장세 필터 (지수 기반)

- `fdr.DataReader('KS11', ...)`(KOSPI), `fdr.DataReader('KQ11', ...)`(KOSDAQ)
  로 지수 데이터 조회
- 종목이 KOSPI 소속이면 KOSPI 지수, KOSDAQ 소속이면 KOSDAQ 지수의 200일선·
  기울기로 그 종목의 시장 상태 판정 (기존 개별 종목 200일선 판정을 대체)
- 시장 상태에 따라 **총 투자비중**을 단계적으로 조절 (완전 현금화 대신):
  - 지수 > 200일선이고 기울기 양수: 100%
  - 지수 > 200일선이지만 기울기 음수: 75%
  - 지수 < 200일선: 40%
  - 지수 < 200일선이고 기울기도 음수(뚜렷한 하락): 20%
- KOSPI 소속 종목과 KOSDAQ 소속 종목이 섞인 포트폴리오면 각자의 시장 상태를
  따로 적용(같은 리밸런싱 시점에도 KOSPI 종목군과 KOSDAQ 종목군의 노출 비중이
  다를 수 있음)

### A6. 추세/역추세 신호 분리 (진입 타이밍 보조 — 선택 적용)

A1~A5로 "무엇을 살지"(상대강도 상위 종목)와 "얼마나 살지"(비중)는 결정됩니다.
"정확히 언제 살지"를 다듬는 보조 장치로, 리밸런싱 시점에 신규 편입 종목에
한해 다음 조건 중 하나를 만족할 때까지 매수를 최대 5거래일 유예(그래도 조건을
못 채우면 그냥 리밸런싱 가격에 매수 — 무기한 유예 금지):

- RSI(14)가 최근 5일 내 저점을 찍고 반등 중, 또는
- 종가가 20일선을 상향 회복

이건 기존 `signals/indicators.py`의 RSI/이동평균 계산을 그대로 재사용하되,
"매도 트리거"로는 쓰지 않고 "신규 매수 타이밍만 미세조정"하는 용도로 한정합니다
(제안 문서의 "RSI 과매수는 매도가 아니라 신규 추격매수 보류 신호로만 쓴다"는
아이디어 반영).

---

## Phase B — Phase A 결과를 보고 필요하면

- 비용 스트레스 테스트: `ROUND_TRIP_COST`를 1×/1.5×/2×로 올려 재실행,
  결과가 비용에 얼마나 민감한지 확인 (포트폴리오 엔진은 회전율이 높아서 중요)
- Deflated Sharpe / PBO 등 통계적 엄밀성 지표 — Phase A에서 시험한 파라미터
  조합 수가 적다면(문헌 표준값 위주) 우선순위는 낮지만, 그래도 최종판단 전에
  한 번은 계산해서 "우연히 좋아 보이는 결과"가 아닌지 점검
- 업종 집중도 제한(현재는 시장 구분만 반영)

## Phase C — 보류 (데이터 확보 전까지)

- **재무 품질 필터**: DART(전자공시시스템) OpenAPI는 무료로 발급 가능하지만
  새 API 키 발급(사용자가 직접 가입 필요) + 새 수집 모듈 개발이 필요한 별도
  과제입니다. 필요하다고 판단되면 별도로 진행 여부를 다시 논의합니다.
- **생존편향 완전 제거**: 과거 시점별 실제 상장종목·상장폐지종목 이력을 무료로
  제공하는 데이터원을 찾지 못하면 계속 한계로만 남깁니다.

---

## 검증 방법론 (반드시 지킬 것)

1. **파라미터는 문헌 표준값으로 고정하고 그리드 탐색하지 않는다** — 룩백
   기간(3/6/12개월), ATR 배수(2.5/3.5), 상위 종목 수(15) 등을 데이터에 맞춰
   조정하고 싶은 유혹이 있겠지만, 이번 프로젝트가 `optimize.py`에서 이미 겪은
   과최적화 함정을 되풀이하지 않기 위해 **원칙적으로 고정**한다. 정 조정이
   필요하면 "왜 이 값이 이론적으로 타당한지"를 먼저 설명하고, 조정 후에는
   반드시 워크포워드로 재검증한다.
2. **워크포워드 검증**: `scripts/walk_forward.py`와 같은 원칙으로, 최소
   3개 이상의 서로 다른 시장 국면(예: 2016-2019, 2019-2022, 2022-2025)에
   대해 각각 아웃오브샘플 성과를 낸다. 특정 한 구간에서만 잘 되는 건 신뢰하지
   않는다.
3. **비교 기준**: 단순보유(해당 유니버스 동일가중 평균)와 기존 v1 엔진(3종목)
   양쪽과 나란히 비교한다.
4. **성공 기준**(사용자가 정의한 정의를 그대로 채택):
   - 거래비용 차감 후 **아웃오브샘플 CAGR**을 최대화하되
   - **MDD**와 **종목 집중도**(개별 종목 12%, 업종 상한)를 제한한 상태에서
   - 레버리지나 한 종목 집중으로 과거 수익률만 높이는 방식은 제외
5. **정직한 보고**: Phase A 결과가 v1보다 나빠도 그대로 보고한다. "더 나은
   구조"라는 가설이 틀렸다는 것도 유효한 결론이다.

## 구현 순서 제안

1. `strategy/momentum.py` — 유니버스/상대강도 순위 (A2)
2. `signals/indicators.py`에 ATR 추가 (A3)
3. `backtest/portfolio_engine.py` — 리밸런싱 루프 (A1, A4, A5 통합)
4. A6(진입 타이밍 보조)는 엔진이 안정된 뒤 마지막에 얹기
5. `scripts/portfolio_backtest.py` — CLI로 돌려서 첫 결과 확인
6. 워크포워드 검증 스크립트로 국면별 재검증
7. 결과가 괜찮으면 앱에 새 탭("🧺 포트폴리오 전략")으로 노출, 기존 탭들은
   그대로 유지 (v1을 지우지 않고 v2를 병행 제공 — 사용자가 직접 비교 가능하게)

---

## 다음 단계

이 문서 승인 후 1번(`strategy/momentum.py`)부터 순서대로 구현을 시작합니다.
규모가 커서 여러 세션에 걸쳐 진행될 수 있으며, 각 모듈을 만들 때마다 실제
데이터로 돌려보고 중간 결과를 공유하겠습니다.

---

## 구현 결과 (2026-09-15)

⚠️ **이 절의 수치는 2026-09-16에 "박스권 약점 + 비용 민감도 수정" 절의
변경으로 대체됐습니다** — 엔진에 ADX 필터/순위 이력/소액생략이 추가되면서
아래 수치와 지금 코드의 실제 동작이 달라졌습니다. 최신 결과는 이 문서 맨
아래 "박스권 약점 + 비용 민감도 수정" 절을 보세요. 이 절은 "처음에 어떻게
설계·검증했는지" 기록으로만 남겨둡니다.

Phase A의 A1~A5까지 구현하고 실데이터로 검증했습니다. A6(진입 타이밍 보조)는
엔진이 안정된 뒤 마지막에 얹기로 한 항목이라 아직 구현 전입니다.

- `strategy/momentum.py` — 상대강도 순위 (A2)
- `signals/indicators.py`에 `compute_atr()` 추가 (A3)
- `backtest/portfolio_engine.py` — 리밸런싱 루프, ATR 손절/트레일링, 위험균등
  비중, 지수 기반 장세 필터 (A1, A3, A4, A5)
- `scripts/portfolio_backtest.py` — 단일 기간 CLI
- `scripts/portfolio_regime_check.py` — 서로 다른 국면 여러 구간을 한 번에
  비교하는 CLI (전통적 워크포워드 대신 — 이유는 "검증 방법론" 참고)

### 검증 결과 (유니버스 상위 100개, 보유 15종목, 편도 비용 0.15%)

| 구간 | 전략 누적수익률 | CAGR | MDD | 샤프 | 매매횟수 | ATR손절 | 코스피 단순보유 |
|---|---|---|---|---|---|---|---|
| 2016-2019 (박스권) | -0.1% | -0.0% | -15.5% | 0.05 | 326회 | 255회 | +6.4% |
| 2019-2022 (코로나+반등+2022하락) | +56.5% | +16.1% | -10.2% | 1.16 | 320회 | 260회 | +48.1% |
| 2022-2025 (금리인상+반도체랠리) | +29.2% | +9.0% | -30.0% | 0.62 | 328회 | 260회 | -19.7% |
| **2016-2025 (전체)** | **+285.9%** | **+13.5%** | **-34.1%** | **0.80** | 1174회 | 933회 | +249.3% |

**정직한 평가**:
- 전체 구간에서는 단순보유를 웃돌고(+285.9% vs +249.3%), 특히 2022-2025
  하락·급등이 섞인 구간에서 단순보유(-19.7%)를 크게 앞섰습니다(+29.2%) — v1
  워크포워드 검증과 달리 v2는 파라미터를 전혀 튜닝하지 않았는데도 이 정도
  결과가 나왔다는 점에서 과최적화로 인한 착시일 가능성은 낮다고 봅니다.
- 다만 **2016-2019처럼 방향성 없는 박스권 구간에서는 거의 이익을 못 냈고
  (-0.1%), 매매의 78%가 ATR 손절로 청산**됐습니다 — ATR 배수(2.5/3.5)가
  이 시장 특성에는 다소 타이트해서 잦은 휩쏘(whipsaw)를 만드는 것으로
  보입니다. PROMPT_V2.md 원칙대로 이 배수를 데이터에 맞춰 조정하지는
  않았습니다 — 조정하려면 "왜 이론적으로 타당한지"를 먼저 설명하고 반드시
  재검증해야 합니다(검증 방법론 1번).
- 회전율이 매우 높아(연평균 100회 이상) 거래비용에 특히 민감합니다. 비용
  스트레스 테스트(Phase B)가 이 전략에는 더 중요해 보입니다.
- 생존편향 한계는 v1의 `scripts/universe_backtest.py`와 동일하게 남아있습니다
  — 유니버스가 오늘 시점 시가총액 상위 목록 기준입니다.

### 아직 안 한 것
- A6(진입 타이밍 보조 — RSI/20일선 회복 대기)
- Deflated Sharpe/PBO, 업종 집중도 제한 (Phase B 나머지)
- Phase C(재무 품질 필터, 생존편향 완전 제거)
- 앱에 v2 탭 노출 여부는 이 결과를 사용자와 논의 후 결정

## 비용 스트레스 테스트 결과 (2026-09-16 1차, 이후 "박스권 약점" 절에서 재실행됨)

⚠️ 이 절도 위와 같은 이유로 이후 대체됐습니다 — 아래는 ADX/순위이력/소액생략
수정 **전** 엔진 기준입니다. 최신 비용 스트레스 결과는 맨 아래 절 참고.

회전율이 높은 전략이라(연 100회 이상 매매) 거래비용 가정에 특히 민감할 수
있어서, 왕복 비용을 1배(0.3%, 기본)/1.5배(0.45%)/2배(0.6%)로 올려가며
재실행했습니다(유니버스 상위 100개, 15종목 보유).

**2016-2025 전체 구간**:

| 왕복비용 | 누적수익률 | CAGR | 샤프 |
|---|---|---|---|
| 0.3%(기본) | +288.3% | +13.5% | 0.80 |
| 0.45%(1.5배) | +257.0% | +12.6% | 0.76 |
| 0.6%(2배) | +228.3% | +11.8% | 0.71 |
| (참고) 코스피 단순보유 | +245.4% | — | — |

기본 비용 가정(0.3%)에서는 단순보유(+245.4%)를 앞섰지만, **비용을 2배로
올리면 +228.3%로 단순보유보다 오히려 낮아집니다.** 전체 기간 기준 v2의
우위는 비용 가정에 따라 뒤집힐 수 있을 만큼 얇다는 뜻입니다 — 정직하게
"강건한 우위"라고 부르기 어렵습니다.

**2022-2025 하락장 낀 구간**(v2가 가장 잘 방어했던 구간)은 다릅니다:

| 왕복비용 | 누적수익률 |
|---|---|
| 0.3%(기본) | +29.2% |
| 0.6%(2배) | +24.0% |
| (참고) 코스피 단순보유 | -19.7% |

이 구간의 방어력(단순보유 대비 +40~50%p 차이)은 비용을 2배로 올려도 거의
그대로 유지됩니다 — **"하락장에서 손실을 줄여준다"는 결론은 비용 가정에
강건하지만, "장기적으로 단순보유를 이긴다"는 결론은 비용 가정에 약합니다.**
이건 v1에서 이미 확인된 패턴("수익 극대화보다 하락장 방어에 가깝다")과
같은 결론입니다.

## 생존편향 재확인 (해결 안 됨)

v1의 `scripts/universe_backtest.py`와 동일한 한계가 v2에도 그대로 적용됩니다
— `get_universe()`가 주는 유니버스는 **오늘 시점** 시가총액 상위 목록이라,
과거 리밸런싱 시점에 상위권이었지만 지금은 상장폐지·순위 밖으로 밀려난
종목은 이 백테스트에 전혀 등장하지 않습니다. FinanceDataReader로 과거 시점의
정확한 상장종목·상장폐지종목 이력을 무료로 구할 방법을 찾지 못해서, Phase C로
분류한 대로 **이번에도 해결하지 못했고 해결을 시도하지도 않았습니다** —
어설픈 부분적 보정을 넣느니 한계로 명확히 남겨두는 편이 정직하다고
판단했습니다. 위 수치들은 이 한계를 감안해서 읽어야 합니다.

## 박스권 약점 + 비용 민감도 수정 (2026-09-16, 이후 아래 절에서 일부 되돌려짐)

⚠️ 이 절의 "사용자가 '지금 수정본 유지'를 선택" 부분은 **바로 아래 절에서
뒤집혔습니다** — 다시 물었을 때 사용자가 "수익률"을 택해서 ADX 필터는
뺐습니다. 최종 상태는 맨 아래 "ADX 필터 제거 — 수익률 우선으로 재조정" 절
참고. 이 절은 진단 과정과 처음 결정의 기록으로 남겨둡니다.

사용자가 "박스권에서 약함, 비용 민감 문제를 해결할 방법은?"이라고 물어서
원인을 진단하고 수정했습니다.

**진단**:
1. 장세 필터(A5)는 가격이 200일선 위/아래인지만 본다. 박스권(2016-2019)은
   그 위/아래가 자주 뒤집혀서 이 필터로는 못 걸러진다.
2. 매매의 78~95%가 리밸런싱이 아니라 **ATR 손절 자체**에서 발생한다 — 순위가
   자주 바뀌어서가 아니라 손절선(2.5×/3.5×ATR)이 그 자체로 자주 걸린다.

**적용한 수정** (`backtest/portfolio_engine.py`, `signals/indicators.py` —
전부 데이터에 맞춘 임의 튜닝이 아니라 이론적으로 정당화되는 표준 기법):
1. **ADX(추세강도) 필터**: Wilder 본인이 제시한 경험적 기준(ADX<20 = 무추세)
   으로, 지수 ADX가 20 미만이면 노출 비중에 0.5배를 추가로 곱한다
   (`signals/indicators.py`의 `compute_adx()`, `_trend_multiplier()`).
2. **순위 이력(hysteresis)**: 이미 보유 중인 종목은 순위가 상위 K의 2배
   (`top_k * RANK_CUTOFF_MULTIPLE`) 밖으로 밀려나야 매도 대상이 된다
   (`_select_with_hysteresis()`).
   > ⚠️ 2026-09-20 정정: 이 기준은 원래 `RANK_CUTOFF = 30`이라는 **고정 상수**였다.
   > K=15에서는 2배와 같아서 문서에 적힌 모든 수치(전부 K=15)는 영향이 없지만,
   > K를 바꾸면 "상위 K의 2배"가 아니라 "30위"로 동작하는 버그였다. 지금은 K에
   > 맞춰 자동으로 계산한다. **K≠15로 돌렸던 임시 실험(예: 10종목/20종목 비교)은
   > 컷오프가 30으로 고정된 채였으니 그 수치는 인용하지 말 것** — 필요하면 다시
   > 돌려야 한다.
3. **소액 리밸런싱 생략**: 비중 차이가 포트폴리오 가치의 1.5%
   (`MIN_TRADE_THRESHOLD`) 미만인 기존 보유 종목 미세조정은 생략한다
   (신규 진입·완전 청산에는 적용 안 됨).

**결과** (유니버스 상위 100개, 15종목 보유):

| 구간 | 수정 전 | 수정 후 |
|---|---|---|
| 2016-2019 박스권 | -0.1% / MDD -15.5% / 샤프 0.05 | **+9.8% / MDD -10.4% / 샤프 0.42** |
| 2016-2025 전체 | +288.3% / MDD -34.1% / 샤프 0.80 | +191.8% / MDD -26.3% / 샤프 0.74 |
| 전체기간 코스피 단순보유 | +245.4% | +245.4%(동일) |
| 2022-2025 (2배비용) | +24.0% | +12.0% (단순보유 -19.7% 대비 여전히 압도) |

**뚜렷한 트레이드오프**: 박스권 방어는 확실히 좋아졌지만(샤프 0.05→0.42),
전체 기간 누적수익률이 단순보유보다 낮아지는 대가를 치렀습니다 — ADX
필터가 진짜 상승 추세 구간(2019-2022, 2022-2025)에서도 노출을 깎기
때문입니다. 매매횟수는 15%가량만 줄어서(hysteresis/소액생략은 리밸런싱발
매매만 건드림), ATR 손절 자체가 여전히 매매의 대부분을 차지합니다 — 이걸
더 줄이려면 손절 폭(2.5×/3.5×ATR)을 손봐야 하는데, 이는 "백테스트 결과가
잘 나오는 값"을 고르는 것과 경계가 애매해서 이번엔 건드리지 않았습니다.

**사용자에게 세 가지 선택지**(현재 버전 유지 / 원래 버전으로 되돌리기 /
ATR 손절 폭 자체를 이론적 근거를 세워 재검토)를 제시했고, **"지금 수정본
유지"를 선택**했습니다 — 박스권에서도 크게 잃지 않는 쪽(방어형 성격을
더 뚜렷이)을 우선하기로 함. ATR 손절 폭 재검토는 보류 상태로 남아있습니다.

## ADX 필터 제거 — 수익률 우선으로 재조정 (2026-09-16, 최종 상태)

사용자가 샤프 비율의 의미(0.05→0.42가 등급표 기준 둘 다 "약함" 구간이고,
3년 표본으로는 통계적으로 유의미한 차이인지도 불확실하다는 점)를 짚었고,
이어서 "박스권 방어 vs 전체 수익률" 질문에 **다시 답한다면 "수익률"**을
택하겠다고 밝혔습니다. 이에 따라 **ADX 무추세 필터(`_trend_multiplier()`
호출)를 리밸런싱 로직에서 뺐습니다** — 함수 자체는 `backtest/portfolio_engine.py`
에 남겨뒀고(나중에 다시 켤 수 있게), 왜 안 쓰는지도 그 자리에 주석으로
남겼습니다. 순위 이력(hysteresis)·소액 리밸런싱 생략은 회전율만 줄이고
수익을 깎지 않는 것으로 확인돼서 그대로 유지했습니다.

**결과** (유니버스 상위 100개, 15종목 보유) — 세 버전 비교:

| 구간 | ①최초(수정 전) | ②ADX 추가(박스권 우선) | ③ADX 제거(현재, 수익률 우선) |
|---|---|---|---|
| 2016-2019 박스권 | -0.1% / 샤프 0.05 | +9.8% / 샤프 0.42 | **+10.5% / 샤프 0.39** |
| 2016-2025 전체 | +288.3% / MDD -34.1% / 샤프 0.80 | +191.8% / MDD -26.3% / 샤프 0.74 | **+348.4% / MDD -32.9% / 샤프 0.88** |
| 전체기간 코스피 단순보유 | +245.4% | +245.4% | +245.4% |
| 전체기간 2배비용 | +228.3%(단순보유 밑돎) | +158.6%(단순보유 밑돎) | **+290.8%(단순보유 여전히 이김)** |

**뜻밖의 결과**: ③(ADX만 뺀 버전)이 박스권·전체수익률·MDD·샤프·비용강건성
**전 항목에서 ①(최초 버전)보다도 낫습니다.** 즉 hysteresis/소액생략만으로도
불필요한 매매(과최적화 없이도 걸러낼 수 있는 "명백히 낭비인" 매매)가
줄어서 전반적으로 개선됐고, ADX 필터가 오히려 그 개선분의 일부를 깎아먹고
있었던 셈입니다. 지금 코드(`backtest/portfolio_engine.py`)는 이 ③ 상태가
최종입니다.

⚠️ **주의**: 이 비교도 3년~10년짜리 소수의 역사적 경로 하나에 대한 결과일
뿐입니다. 샤프 비율처럼 표본이 작은 지표는 추정 오차가 커서, ①→③의 차이가
실제로 "이 변경이 항상 더 낫다"를 보장하지 않습니다. 다음에 이 프로젝트로
돌아왔을 때 이 결론에 너무 의존하지 말고, 기회가 되면 워크포워드 방식(다른
시작점/다른 유니버스 크기로 반복)으로 이 개선이 여러 조건에서도 재현되는지
확인하는 게 좋습니다 — 아직 하지 않았습니다.

**ATR 손절 폭(2.5×/3.5×) 자체는 여전히 손대지 않았습니다** — 매매의
대부분을 차지하는 진짜 원인이지만, "결과가 잘 나오는 값 고르기"와 경계가
애매해서 이론적 근거 없이는 건드리지 않기로 한 원칙을 유지했습니다.
