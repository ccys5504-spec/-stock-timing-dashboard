# 2026-09-20: 검증(백테스트/테스트)을 마친 버전으로 고정했다. 예전엔 `>=` 범위라
# 재배포할 때마다 라이브러리가 조용히 바뀌어 "어제와 다른 결과"가 나올 수 있었다.
# 올릴 때는 한 번에 하나씩 올리고 `python -m pytest`를 통과시킨 뒤 커밋할 것.
streamlit==1.63.0
pandas==2.3.3  # 3.x는 pyarrow를 무조건 요구함(버전만 확인, 예외처리 없음).
# numpy 2.2.6은 Python 3.14용 배포판이 없어서, 3.14에서만 가장 가까운 2.3.5를 쓴다.
numpy==2.2.6; python_version < "3.14"
numpy==2.3.5; python_version >= "3.14"
plotly==5.24.1
finance-datareader==0.9.202
ta==0.11.0
openpyxl==3.1.5  # 스캔 결과 엑셀(.xlsx) 내보내기용
requests==2.34.2  # 관심종목/보유종목을 GitHub Gist에 저장할 때 사용 (data/fetch.py)
pyarrow==25.0.1  # Streamlit이 데이터프레임 표시에 필요. 이 컴퓨터는 Windows 앱 제어
                 # 정책이 pyarrow의 네이티브 계산 모듈(.compute)만 차단하는데,
                 # pandas가 그걸 무조건 import하려 들어서 `import pandas`가 실패한다.
                 # `_pyarrow_compat.py`가 이럴 때만 `.compute`를 빈 더미로 대체해서
                 # 우회한다 — app.py/scripts 맨 위에서 pandas보다 먼저 import해야 함.
                 # 이 우회가 막는 건 pyarrow.compute뿐이고, Streamlit이 실제로 쓰는
                 # pyarrow.Table 등은 정상 동작한다.
